# 04AD_P8_TRAINING_SEED_PROTOCOL_COMPARISON_IMPLEMENTATION_REVIEW

## Decision

```text
A. Review can close.

ACCEPTED as current-scope complete:
the exact `04AC` selected-versus-contiguous training-seed comparison.

APPROVED for next exact implementation task:
train on every predeclared seed 0..31 in order and evaluate before/after with
zero updates on disjoint seeds 52..83.
```

Zero planning, proposal or review gates remain before that code.

## Reviewed Commit

```text
ebc974c  Compare MahJax training seed protocols
```

## Conformance Review

- Only the two exact `04AC` files plus direct governance changed.
- The approved eight-symbol public surface is frozen and array-free.
- Two branches start from identical reviewed imitation parameters.
- Exact outcome-selected `(1,3,5,7,11)` and contiguous `(0,1,2,3,4)` tuples
  each retain five legal terminal update attempts at the reviewed `0.01` rate.
- Contiguous zero-return seeds `(0,2,4)` remain exact zero-objective,
  zero-gradient, zero-delta no-op attempts; they are not filtered or replaced.
- Both branches use identical zero-update fixed evaluation on seeds `20..51`.
  Initial/selected/contiguous sums are `-501/-650/-501`; changed seeds are
  `(32,39,43,44,50)/()` and positive/negative counts are `0/18` versus `1/16`.
- `selected_protocol_id` is `None`. No third protocol, adaptive search,
  persistence, path, real-data, production evaluation or P9-P12 path exists.

No code or test blocker was found.

## Validation

```text
9 focused tests OK in 389.507 seconds
468 explicit repository tests OK in 3482.351 seconds; 2 existing skips
compileall pass
pip check: no broken requirements
git diff/check pass
independent full predeclared-range training/evaluation probe pass
```

## Independent Full-Range Probe

The probe starts from the same reviewed imitation parameters, attempts one
reviewed update for every seed in exact order `0..31`, retains zero-return
attempts, and evaluates initial/final parameters without updates on disjoint
seeds `52..83`.

```text
training seeds: 0..31
update attempts: 32
nonzero updates: 10
nonzero seeds: (1,3,5,7,11,17,25,26,27,31)
zero/no-op seeds: (0,2,4,6,8,9,10,12,13,14,15,16,18,19,20,21,22,23,24,28,29,30)
final parameter L2 deltas: (0.0033048680,0.0006281338,0.0085437289,0.0009697253)

evaluation seeds: 52..83
initial/final raw sums: -312 / -312
initial/final positive rounds: 2 / 2
initial/final negative rounds: 20 / 20
changed evaluation seeds: (52,65,72)
```

The sequential nonzero set differs from the frozen-policy census because later
trajectories are generated by updated parameters. The fixed raw-reward vector
is unchanged even though three complete evaluation records change. This is
behavior-change and signal-sparsity evidence, not improvement.

## Direct Full-Range Implementation Approval

Exact files:

```text
src/mjlabai/rl/mahjax_categorical_mlp_predeclared_census_training_evaluation_smoke.py
tests/rl/test_mahjax_categorical_mlp_predeclared_census_training_evaluation_smoke.py
```

Direct governance synchronization is allowed. Existing source/test logic and
public APIs must not change.

Implementation must:

1. load reviewed imitation parameters once and train one in-memory branch;
2. attempt one reviewed actor-indexed raw-return update at `0.01` for every
   exact seed `tuple(range(32))` in order, with no filtering or replacement;
3. retain all 32 legal terminal trajectories and pin transition counts, raw
   returns, scores, trace digests, objectives and parameter deltas;
4. pin exact 10 nonzero and 22 zero/no-op attempts listed above;
5. evaluate initial/final parameters with zero updates on exact disjoint seeds
   `tuple(range(52,84))` under identical environment/rule RNG;
6. pin raw sums `-312/-312`, counts and changed seeds `(52,65,72)`;
7. return frozen array-free diagnostics and no selected checkpoint/protocol;
8. label changed behavior without reward change as diagnostic evidence only.

## Forbidden Scope

- no filtering, replacement, shuffle, replay, epoch, second pass or extra seed;
- no learning-rate, estimator, optimizer, critic, reward or algorithm change;
- no checkpoint/model selection, persistence, artifact, path or CLI;
- no real/external data, Tenhou, production self-play/evaluation or league;
- no improvement, superiority, stable-dan, LuckyJ or promotion claim;
- no P9-P12.

## Evidence Grade

```text
P8 exact implementation-review and predeclared full-range training diagnostic
approval evidence only; not policy-quality or strength evidence.
```
